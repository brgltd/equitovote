import { Metadata } from "next";
import Page from "./page";

export const metadata: Metadata = {
  title: "Equito Vote | Add New Token",
  description: "Add a new token to the Equito Vote token registry.",
};

export default Page;
